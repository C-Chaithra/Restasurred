package steps;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class GoogleSearchSteps {

	/*
	 * WebDriver driver = null;
	 * 
	 * @Given("browser window is open") public void browser_window_is_open() {
	 * System.out.println("In step-browser window is open");
	 * 
	 * driver = new ChromeDriver();
	 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(40));
	 * driver.manage().timeouts().pageLoadTimeout(Duration.ofSeconds(40)); }
	 * 
	 * @And("user is on google search page") public void
	 * user_is_on_google_search_page() {
	 * System.out.println("In step-user is on google search page");
	 * driver.navigate().to("https://google.com"); }
	 * 
	 * @When("user enters a text in search box") public void
	 * user_enters_a_text_in_search_box() {
	 * System.out.println("In step-user enters a text in search box");
	 * 
	 * driver.findElement(By.name("q")).sendKeys("Automation Step by Step");
	 * 
	 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(40)); }
	 * 
	 * @And("hits enter") public void hits_enter() {
	 * System.out.println("In step-hits enter");
	 * driver.findElement(By.name("q")).sendKeys(Keys.ENTER);
	 * driver.manage().timeouts().implicitlyWait(Duration.ofMinutes(1)); }
	 * 
	 * @Then("user is navigated to search results") public void
	 * user_is_navigated_to_search_results() {
	 * System.out.println("In step-user is navigated to search results");
	 * driver.manage().timeouts().implicitlyWait(Duration.ofMinutes(1));
	 * 
	 * driver.getPageSource().contains("Online Courses"); driver.close();
	 * driver.quit(); }
	 */
}